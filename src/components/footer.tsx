import { FunctionalComponent } from 'preact'
import styles from './footer.module.scss'

const Footer: FunctionalComponent = () => {
  return <footer class={styles.footer}>footer</footer>
}

export default Footer
